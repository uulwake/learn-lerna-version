{
  "name": "backend",
  "devDependencies": {
    "generate-changelog": "^1.8.0",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "create": "lerna create $1 -y",
    "version": "lerna version --yes --no-changelog",
    "generate-changelog": "generate changelog $1"
  }
}
